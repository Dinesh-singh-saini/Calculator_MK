<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Math_catcutate" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Math_catcutate" parent="Base.Theme.Math_catcutate" />
    <style name="combtn" parent="Widget.MaterialComponents.ExtendedFloatingActionButton">

        <item name="android:layout_width">90dp</item>
        <item name="android:layout_height">90dp</item>
        <item name="android:textColor">@color/cmn</item>
        <item name="android:backdropColor">@color/white</item>
        <item name="cornerRadius">50dp</item>
        <item name="android:stateListAnimator">@null</item>
        <item name="android:textSize">48dp</item>
        <item name="android:layout_margin">5dp</item>
        <item name="android:backgroundTint">@color/white</item>
    </style>
    <style name="calculate">
        <item name="android:layout_marginEnd">36dp</item>
        <item name="android:gravity">end</item>
    </style>
</resources>